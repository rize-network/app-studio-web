# ProgressBar

A visual indicator displaying the progress of an operation, customizable in appearance.

### **Import**
```tsx
import { ProgressBar } from '@app-studio/web';
```

### **Default**
```tsx
import React from 'react';
import { ProgressBar } from '@app-studio/web';

export const DefaultDemo = () => <ProgressBar value={50} />;
```

