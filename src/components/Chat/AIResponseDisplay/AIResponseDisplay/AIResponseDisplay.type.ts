/**
 * AIResponseDisplay Types
 */

import { ViewProps } from 'app-studio';

export interface AIResponseDisplayStyles {
  container?: ViewProps;
  text?: ViewProps;
  codeBlock?: ViewProps;
  citation?: ViewProps;
}
