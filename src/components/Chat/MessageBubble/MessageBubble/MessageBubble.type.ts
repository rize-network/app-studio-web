/**
 * MessageBubble Types
 */

import { ViewProps } from 'app-studio';

export interface MessageBubbleStyles {
  container?: ViewProps;
  content?: ViewProps;
  avatar?: ViewProps;
  timestamp?: ViewProps;
  actions?: ViewProps;
}
