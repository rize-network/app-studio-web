import { ViewProps } from '../../View/View';
// Defines the type for Center component props, extending from ViewProps to inherit properties of a generic view component.
export interface CenterProps extends ViewProps {}
