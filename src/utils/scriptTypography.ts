type ScriptTypography = Record<string, Record<string, number>>;
export const subScriptTypography: ScriptTypography = {
  fontSizes: {
    xs: 7, // Harmonized for 10px base
    sm: 9, // Harmonized for 12px base
    md: 10, // Harmonized for 14px base
    lg: 12, // Harmonized for 16px base
    xl: 15, // Harmonized for 20px base
  },
};

export const supScriptTypography: ScriptTypography = {
  fontSizes: {
    xs: 7, // Harmonized for 10px base
    sm: 9, // Harmonized for 12px base
    md: 10, // Harmonized for 14px base
    lg: 12, // Harmonized for 16px base
    xl: 15, // Harmonized for 20px base
  },
};
